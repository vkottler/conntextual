---
includes:
  - package://conntextual/sample.yaml

app:
  - conntextual.ui.test

config:
  debug: true
  headless: true

  tab_pattern:
    include: ".*"

  channel_patterns:
    sample:
      include: ".*"
    udp_json_server:
      include: "metrics"
      exclude: "tx"
